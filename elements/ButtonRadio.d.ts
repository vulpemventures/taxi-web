import React from 'react';
interface Props {
    defaultIndex?: number;
    onChange: any;
    values: Array<string>;
}
declare const Button: React.FunctionComponent<Props>;
export default Button;
